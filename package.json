{
  "name": "replang-infra",
  "version": "0.1.0",
  "private": true,
  "description": "Repo chef d'orchestre Replang : docker-compose, schéma BDD source de vérité, packages partagés (@replang/db, @replang/shared), docs et scripts.",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @replang/db && npm run build -w @replang/shared",
    "db:generate": "npm run generate -w @replang/db",
    "db:migrate": "npm run migrate -w @replang/db",
    "db:migrate:deploy": "npm run migrate:deploy -w @replang/db",
    "db:studio": "npm run studio -w @replang/db",
    "db:seed": "npm run seed -w @replang/db"
  },
  "license": "UNLICENSED"
}
